server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    location /store {
        return 301 https://admin.blessedave.com/store;
    }
    location / {
        try_files $uri $uri.html $uri/ /404.html;
    }
}
